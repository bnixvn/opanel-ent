<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ domain }} — OPanel Enterprise</title>
    <style>
        body { font-family: system-ui, sans-serif; background: #f8fafc; color: #0f172a; display: grid; place-items: center; min-height: 100vh; margin: 0; }
        main { max-width: 480px; padding: 32px; text-align: center; }
        h1 { font-size: 24px; margin-bottom: 8px; }
        p { color: #64748b; }
    </style>
</head>
<body>
    <main>
        <h1>{{ domain }}</h1>
        <p>This site is managed by OPanel Enterprise. Upload your files to get started.</p>
    </main>
</body>
</html>
