#
# Generated by OPanel. Do not edit: this file is rewritten
# from the panel database on every change, and edits made here
# or in the WebAdmin console will be lost.
#
# Site: static.example.org  (static)
# Owner: carol
#

docRoot                       /home/carol/static.example.org/public_html
enableGzip                    1
enableBr                      1

errorlog /home/carol/static.example.org/logs/error.log {
    useServer                 0
    logLevel                  ERROR
    rollingSize               10M
    keepDays                  30
}

accesslog /home/carol/static.example.org/logs/access.log {
    useServer                 0
    rollingSize               10M
    keepDays                  30
    compressArchive           1
}

index {
    useServer                 0
    indexFiles                index.html, index.htm
    autoIndex                 0
}

expires {
    enableExpires             1
}


context / {
    location                  /home/carol/static.example.org/public_html
    allowBrowse               1
    # .htaccess is how WordPress and most PHP applications express their own
    # rewrite rules, so it is honoured rather than replaced.
    rewrite {
        enable                1
        allowOverride         1
    }
    addDefaultCharset         off
}

# Certificate renewals must keep working while the site is live, and while it
# is suspended: a suspension is not a reason to let the certificate lapse.
context /.well-known/acme-challenge {
    location                  /usr/share/opanel/acme/.well-known/acme-challenge
    allowBrowse               1
}
