#
# Generated by OPanel. Do not edit: this file is rewritten
# from the panel database on every change, and edits made here
# or in the WebAdmin console will be lost.
#
# Site: suspended.example.com  (php, PHP 8.4)  [SUSPENDED]
# Owner: erin
#

docRoot                       /usr/share/opanel/suspended
enableGzip                    1
enableBr                      1

errorlog /home/erin/suspended.example.com/logs/error.log {
    useServer                 0
    logLevel                  ERROR
    rollingSize               10M
    keepDays                  30
}

accesslog /home/erin/suspended.example.com/logs/access.log {
    useServer                 0
    rollingSize               10M
    keepDays                  30
    compressArchive           1
}

index {
    useServer                 0
    indexFiles                index.php, index.html, index.htm
    autoIndex                 0
}

expires {
    enableExpires             1
}


# Suspended: the document root is swapped for the shared notice page and no
# interpreter is configured, so nothing of the customer's application runs.
# The vhost itself stays in place, making unsuspend a single re-render.
context / {
    location                  /usr/share/opanel/suspended
    allowBrowse               1
    indexFiles                index.html
}
