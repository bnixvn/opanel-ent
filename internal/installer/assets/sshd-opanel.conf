# Managed by OPanel.
#
# Global directives only. Match blocks must NOT go in this file: sshd reads
# the Include near the top of sshd_config, so a Match here would swallow the
# rest of the main file. The SFTP Match block is appended to sshd_config
# itself instead.
Subsystem sftp internal-sftp
