# Databases
*.db
*.sqlite
*.sqlite3

# Node
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# Env / secrets
.env
.env.local
*.pem
*.key

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
.kiro/

# Build artifacts
*.bak
*.tar.gz
backup/

# Dev / CI / tooling (not part of release)
.codex/
.github/
# ...except the CI workflows, which have to be committed to run at all.
!.github/
!.github/workflows/
!.github/workflows/*.yml
.ruff_cache/
.vendor/
skills/
prompts/

# Codebase memory
.codebase-memory/

# Go build output
/dist/

# Frontend build inputs. The built output under internal/httpapi/web IS
# committed: the Go binary embeds it, so `go build` has to work for anyone
# who clones this without running npm first.
web/node_modules/
